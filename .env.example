# 🔐 Tu clave de API de OpenAI
OPENAI_API_KEY=sk-proj-tu_clave_aqui

# 🧠 ID del Assistant creado en OpenAI (desde la plataforma o API)
ASSISTANT_ID=asst_tu_assistant_id_aqui

# 📦 Dirección del servidor de Qdrant (usualmente localhost si lo corres tú)
QDRANT_URL=http://localhost:6333

# 📚 Nombre de la colección en Qdrant (debe coincidir con la que creaste)
COLLECTION_NAME=material_educativo

# 🌐 Puerto donde se ejecutará el WebSocket server
WS_PORT=8080

# 💾 Ruta a la base de datos SQLite (opcional)
DB_PATH=educational_threads.db

# 🌎 Región de AWS si usas embeddings de Bedrock (opcional)
AWS_REGION=us-east-1
AWS_PROFILE=tu_perfil_aws